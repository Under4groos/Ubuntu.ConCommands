# ufw
``` 
1. sudo apt install nginx - установка 
2. sudo ufw status - инфрмация о текущих открытых портах 
3. sudo ufw allow <port> - добавляем порт
------------
sudo ufw allow "Nginx Full"
sudo ufw allow 22
sudo ufw allow 80

```